const { expect } = require('chai');
const Stmt2Ary = require("../src/stmt2ary").default;
/*
describe("stmt2ary", () => {
    describe("main", () => {
        it("top", () => {
            
            
            expect(res.length)
                .to.equal(exp.length);
            res.forEach((r, i) => {
                expect(r)
                    .to.equal(exp[i]);
            });
        });
    });
});

*/
